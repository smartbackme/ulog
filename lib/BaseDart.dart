import 'package:flutter/material.dart';

class BaseDart extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return new Scaffold(appBar: AppBar(
      title: Text("BaseDart"),
    ),body:
    Center(
      child: Text("This is new Router"),
    )
      ,);
  }

}